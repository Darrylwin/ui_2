import 'package:flutter/material.dart';

class EducationPag extends StatelessWidget {
  const EducationPag({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("EduPage"),
      ),
    );
  }
}
